import type { HomeSummary } from './home.types';

export const mockHomeSummary: HomeSummary = {
  id: 'home-summary-1',
  activeListings: 4,
  unreadMessages: 2,
  weatherSummary: 'Partly cloudy, 28°C',
};
